# file: count.calc
# purpose: Count according to user specifications
# javac -d . *.java

input start
input count_end
input increment

num = start
repeat num <= count_end
  display num
  num = num + increment
end repeat